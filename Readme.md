# ☕ 歐舍咖啡線上菜單

一個現代化、多語言的線上咖啡菜單系統，整合 Google Sheets 作為資料來源，支援 QR Code 掃描使用。

## ✨ 功能特色

- 🌍 **多語言支援**：繁體中文、英文、粵語、韓語、日語
- 📱 **響應式設計**：完美支援手機、平板、電腦
- 🔄 **即時更新**：透過 Google Sheets 管理菜單內容
- 🎨 **動態配色**：每個類別都有專屬的品牌色
- 🖼️ **圖片放大**：點擊圖片可放大檢視
- 🔍 **搜尋功能**：快速找到想要的咖啡
- 📊 **分類篩選**：手沖豆單支援莊園級、競賽級等分類

## 📋 菜單類別

1. **手沖豆單** - 精選單品咖啡豆

   - 莊園級 ($150/杯)
   - 競賽級 ($200/杯)
   - 瑰夏 ($300/杯)
   - 限量版

2. **義式咖啡** - 經典義式咖啡飲品

3. **特調系列** - 特色手作飲品

4. **其他飲品** - 非咖啡飲品選擇

   - 支援無咖啡因篩選

5. **當日點心** - 精選手作甜點

## 🚀 技術架構

- **前端**: 純 JavaScript (Vanilla JS)
- **樣式**: CSS3 + CSS Variables
- **資料來源**: Google Sheets (CSV 匯出)
- **部署**: GitHub Pages
- **圖片管理**: GitHub Repository

## 📦 檔案結構

```
.
├── index.html              # 主頁面
├── favicon.svg             # 網站圖示
├── css/                    # 樣式檔案
│   ├── common.css          # 通用樣式
│   ├── product-card.css    # 產品卡片樣式
│   └── desserts.css        # 點心卡片樣式
├── js/
│   ├── common/             # 核心功能
│   │   ├── app.js          # 應用程式主邏輯
│   │   ├── config.js       # 設定檔
│   │   ├── i18n.js         # 多語言翻譯
│   │   ├── ui.js           # UI 更新
│   │   └── utils.js        # 工具函式
│   └── modules/            # 各類別渲染器
│       ├── beverages.js    # 飲品
│       ├── desserts.js     # 點心
│       ├── espresso.js     # 義式咖啡
│       ├── pourover.js     # 手沖
│       └── signature.js    # 特調
└── images/                 # 產品圖片
```

## 🔧 本地開發

1. Clone 專案：

```bash
git clone https://github.com/你的帳號/orsir-coffee-menu.git
cd orsir-coffee-menu
```

2. 使用本地伺服器運行（推薦使用 VS Code 的 Live Server 擴充功能）

3. 開啟 `http://localhost:5500` 查看

## 📝 資料管理

### Google Sheets 結構

每個類別都有獨立的 Sheet，包含以下欄位：

**共同欄位**：

- `id` - 唯一識別碼
- `name_zh` - 中文名稱
- `name_en` - 英文名稱
- `name_yue` - 粵語名稱
- `name_ko` - 韓語名稱
- `name_ja` - 日語名稱
- `tags_zh` - 中文標籤（用 `|` 分隔）
- `tags_en` - 英文標籤
- `description_zh` - 中文描述
- `description_en` - 英文描述
- `price` - 價格
- `image_name` - 圖片檔名
- `is_featured` - 是否精選 (TRUE/FALSE)
- `is_sold_out` - 是否售完 (TRUE/FALSE)

**手沖豆單額外欄位**：

- `category` - 分類 (estate/competition/geisha/limited)
- `roast` - 烘焙度 (Light/Medium/Medium-Dark/Dark)
- `taste` - 風味描述
- `origin` - 產區
- `variety` - 品種
- `altitude` - 海拔

**其他飲品額外欄位**：

- `caffeine_free` - 是否無咖啡因 (TRUE/FALSE)

### 更新菜單

1. 前往 Google Sheets 編輯內容
2. 檔案 → 發布到網路 → 選擇工作表 → CSV
3. 複製 CSV 連結
4. 更新 `js/common/config.js` 中的 `DATA_SOURCES`

## 🎨 客製化

### 修改品牌色

在 `css/common.css` 的 `:root` 修改：

```css
:root {
  --brand: #85a817; /* 主品牌色 */
  --bg: #f3f6ec; /* 背景色 */
  --card: #fff; /* 卡片背景 */
  --text: #3d2f27; /* 主文字色 */
}
```

### 修改類別配色

在 `js/common/app.js` 的 `COLOR_MAP` 修改：

```javascript
const COLOR_MAP = {
  pour_over: ["#c7a77f", "#f7f1eb", "#fff"],
  espresso: ["#503629", "#eae7e5", "#fff"],
  // [主色, 漸層色, 文字色]
};
```

## 📱 QR Code 產生

部署完成後，使用以下方式產生 QR Code：

1. **線上工具**: [QR Code Generator](https://www.qr-code-generator.com/)
2. **Google API**:

```
https://chart.googleapis.com/chart?cht=qr&chs=300x300&chl=你的網址
```

## 🐛 除錯

開啟瀏覽器的開發者工具 (F12)，查看 Console 的訊息：

- `✅ Loaded X items from GSheet` - 資料載入成功
- `📋 Headers found:` - 顯示欄位名稱
- `🔍 First item:` - 顯示第一筆資料

## 📄 授權

Copyright © 2002-2025 歐舍咖啡

## 🙏 致謝

感謝所有為這個專案貢獻的人員。

---

**享受您的咖啡時光！** ☕
